"use client";

import { Metadata } from "next";

export const metadata:Metadata = {
  title: {
    template: "%s | MapleM",
    default: "Loading..."
  },
  description: 'MapleM information check site',
}